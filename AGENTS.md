# AGENTS.md — 主流 AI Agent 适配说明

> 本文件让任何支持 `AGENTS.md` / `CLAUDE.md` / `.cursorrules` 的 AI 编码工具按同一协议工作。
> 协议本体在 `SKILL.md`，本文件是各 agent 的接入层。

## 你在执行什么

用户（产品经理）提出**交互原型 / 页面设计**需求。你的职责：**先通过多轮澄清完全理解用户的设计意图，再基于设计 token 出稿**。

核心铁律：**未充分理解交互细节前，禁止出稿。** 宁可多问三轮，不可早出一版错的。

## 启动协议（每次使用必读）

1. 读取 `SKILL.md`（主协议）与 `references/问题库全表.md`（问题素材）
2. 读取 `references/design-tokens.md`（预置设计 token，**仅当用户没有自己的 token 时使用**）
3. 按下方「5 步流程」执行

## 5 步流程

```
① 复述理解   —— 收到设计稿/截图/诉求后，先输出：页面结构 + 交互要点 + 不确定项
② 逐项澄清   —— 一次一问（绝不抛清单），用问题库 + 四种提问哲学
③ 共识确认   —— 复述最终理解，用户确认后才进入设计
④ 执行设计   —— 先问设计 token → 用户自备优先，无则用预置 → 出稿
⑤ 交付       —— 线框图 / 高保真原型 / 评审报告
```

## 设计 token 询问（每次必做）

进入设计前，先问：**「你有自己的设计 token 吗？」**

- 用户给了（粘贴值 / 上传文件 / 提供品牌官网）→ 解析并全量替换
- 用户没给 → 用 `references/design-tokens.md` 预置值，交付注明「默认 token」
- 换 token 后校验 WCAG AA 对比度（主色 vs 文字，深浅双主题）

## 提问哲学（四种）

1. **苏格拉底式追问**——从答案反推前提，挖隐含假设
2. **对抗式审查**——替用户找茬（新手能找到入口吗？误点了会怎样？）
3. **交互断点扫描**——拆任务路径找卡点（找不到入口 / 看不到状态 / 无法撤销）
4. **友好性检查**——可撤销 / 防误触 / 100ms 反馈 / 命名一致 / 渐进披露

依据：Nielsen 10 启发式、Hick 定律、Fitts 定律、心智模型。

## 交付三档（按用户需求）

| 档位 | 产出 | 说明 |
|---|---|---|
| L1 线框图 | .excalidraw | 可拖入 excalidraw.com 二次编辑 |
| L2 高保真 | 可交互 HTML | token 落地、深浅双主题、全状态 |
| L3 评审报告 | 问题清单+建议 | 不改稿 |

## 各 Agent 接入

### Cursor

在项目根目录创建 `.cursor/rules/pm-prototype.mdc`（或直接引用本文件）：

```
读取 AGENTS.md，按其中的启动协议与 5 步流程执行交互原型设计。
```

### Claude Code

在 `CLAUDE.md` 中追加：

```
## PM-Prototype-Skill
读取 pm-prototype-skill/AGENTS.md，按启动协议执行交互原型设计任务。
```

### Codex（OpenAI）

对话开头引用：`按 pm-prototype-skill/AGENTS.md 的协议执行。`

### Cline / 其他 MCP 型 agent

把 `pm-prototype-skill/` 目录加入上下文，或复制 `AGENTS.md` 到项目根目录（AGENTS.md 是通用约定）。

## 反模式（禁止）

- 未确认理解就出稿
- 一次抛多个问题
- 忽视用户中途补充（补充 = 修正理解，不重新规划）
- 用历史项目配色套新设计（每次问 token）
- 把「模式入口」误读为「普通设置项」而不追问
- 把设备参数与模式混为一谈而不确认数据归属
