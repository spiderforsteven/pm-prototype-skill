---
name: pm-prototype-skill
description: "Use when 交互原型/页面设计需求。强制先多轮澄清理解再出稿，默认欧美 App 风格。"
version: 1.2.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [原型, 交互设计, prototype, 产品经理, 澄清, 多轮问答]
    related_skills: [maygrove-design-system, mastergo-mcp-handoff, maygrove-wireframe, design-md]
---

# PM-Prototype-Skill 交互原型设计工作流

面向产品经理的交互原型设计 skill。用户通过「设计软件 MCP / 截图 + 优化诉求 / 纯文字诉求」发起需求，我按**强制协议**执行：**多轮澄清理解 → 复述确认 → 加载设计规范 → 分层设计 → 交付**。

**核心铁律：未充分理解交互细节前，禁止出稿。** 宁可多问三轮，不可早出一版错的。

## 1. 工作流总览（5 步）

```
输入（MCP / 截图 / 诉求）
  ↓
① 复述理解   —— 页面结构 + 交互要点 + 不确定项（三块复述）
  ↓
② 逐项澄清   —— 一次一问（用户偏好），按问题库 + 提问哲学追问
  ↓
③ 共识确认   —— 复述最终理解，用户确认后才进入设计
  ↓
④ 执行设计   —— 先问设计token（用户自备优先）→ 无则用预置 → L1线框图 / L2高保真 / L3评审报告
  ↓
⑤ 交付       —— 中文文件名 + hermes_delivery/ + Pages 部署（可选）
```

## 2. 输入通道处理

| 输入类型 | 处理方式 |
|---|---|
| 设计软件 MCP（如 MasterGo） | 按 `mastergo-mcp-handoff` 读 DSL → 生成结构理解草稿 |
| 截图 + 优化诉求 | vision 读图 → 生成结构理解草稿 |
| 纯文字优化诉求 | 直接进入澄清 |
| **已有实现 vs 设计稿对齐**（用户说“样式改了/先告诉我哪里不一致”） | 先执行「差异审计」，列出结构化不一致清单，确认后再修改 |

读图/读稿后，**第一步永远输出「复述理解」**，格式：

```
## 我读到的设计稿
- 页面类型：……
- 结构：自上而下 …
- 交互要点：……（入口、跳转、状态、反馈）
- 我理解的模式/状态体系：……
- ⚠️ 我不确定：……（列出所有存疑点）
```

## 3. 提问哲学（核心方法论）

问题库（见 references/问题库全表.md）只是素材。提问时遵循四种哲学，确保问题穷尽：

### 3.1 苏格拉底式追问（Socratic Probing）
从用户的答案反推前提，连续追问「为什么」，挖出隐含假设。
- 句式模板：「你提到 X，是因为 Y 吗？」「如果 X 不成立会怎样？」「X 背后想解决什么问题？」
- 目标：把用户没说出口的约束/理由显性化（如：为什么参数要独立？→ 因为切换模式时用户仍需要监控设备）。

### 3.2 对抗式审查（Adversarial Review）
站在「挑剔用户 / 反方」立场挑战每个设计假设，不是质疑用户，而是替用户找茬。
- 句式模板：「假设一个完全不懂的用户，他能找到这个入口吗？」「如果用户误点了会怎样？」「这个功能删掉会损失什么？」
- 目标：暴露设计中的发现性、误操作、信息缺失问题。

### 3.3 交互断点扫描（Interaction Breakpoint Scan）
把用户走完一个任务的路径拆成步骤，逐点检查「会不会卡住 / 困惑 / 放弃」。
- 断点类型：入口找不到 / 不知道当前状态 / 不确定操作结果 / 无法撤销 / 信息过载 / 返回迷失。
- 句式模板：「用户从打开页面到完成任务，第一步是什么？最后一步是什么？中间有几步可能卡住？」

### 3.4 交互友好性检查（Interaction Friendliness）
覆盖误操作防护、可恢复性、反馈及时性、认知负担、学习成本。
- 维度：可撤销（undo）、防误触（二次确认/距离）、反馈（操作后 100ms 内可见变化）、一致性（同一概念全稿同名）、渐进披露（高级功能隐藏不干扰新手）。

### 3.5 交互设计方法论速查（引用依据）
- Nielsen 10 大可用性启发式（状态可见性、系统与现实匹配、用户可控、一致性、防错、识别而非回忆、灵活高效、简约美观、错误恢复、帮助文档）
- 心智模型：用户对系统的预期来自同类产品经验（如「›」=进入详情）
- Hick 定律：选项越多决策越慢 → 一屏一个主操作
- Fitts 定律：触控目标 ≥44pt，间距充足
- 渐进披露：常用功能优先，高级功能藏入二级

## 4. 澄清节奏（用户偏好，必须遵守）

- **一次一问**：绝不一次抛问题清单。答完一个再问下一个。
- 用所在 agent 支持的交互方式提问（Hermes 下用 clarify 可点击选项；其他 agent 用单条消息一问）——绝不一次抛问题清单。
- 优先级：先问影响设计方向的问题（入口/跳转/模式结构/数据归属），再问细节（文案/配色）。
- 用户中途补充信息 = 修正理解，更新理解后继续，**不重新规划、不否定用户**。

## 5. 设计执行

1. **设计 token 询问协议（每次必做）**：先问用户「你有自己的设计 token 吗？」——
   - ① 有：解析用户 token（粘贴值 / 上传文件 / 品牌官网提取配色）并**全量替换**预置值
   - ② 没有：加载预置 `references/design-tokens.md`，交付时注明「默认 token」
   - 优先顺序：**用户 token > 预置**。Maygrove 项目可关联 `maygrove-design-system`，同样遵循此询问协议。
   - **无品牌 token 时的默认 = 欧美 App 通用风格**：以 Apple HIG、Uber、BirdBuddy 为基线（中性灰阶、克制色彩、单一品牌色、深浅双主题、大留白、一屏一主操作）。具体值与自查清单见 `references/design-tokens.md`。
   - **默认稿禁止出现国内促销型 UI 观感**（滴滴/淘宝式红区）：高饱和红橙渐变 CTA、顶部多图轮播 Banner、金币/优惠券/红包/倒计时贴纸、满屏红点数字角标、图标五颜六色、促销弹窗。原型交付前按 design-tokens.md 的「欧美 vs 反例」表自查。
   - 换 token 后必须校验：主色与文字对比度 ≥ WCAG AA（4.5:1），深浅双主题都过。
2. **三档交付**（按用户需求）：
   - L1 低保真线框图：`maygrove-wireframe` 生成 .excalidraw（可二次编辑）
   - L2 高保真可交互原型：HTML（token 落地、深浅双主题、英文/中文按用户、全状态）
   - L3 交互评审报告：问题清单 + 建议（不改稿）
3. **交付物规范**：中文文件名、`hermes_delivery/`、可部署 Pages（krisays2 子目录 + publish.yaml cp 行 + 根 index.html 卡片 + curl 200 验证）。
4. **验证**：交互用浏览器事件派发实测（点击/跳转/Toast/状态切换）；深浅模式渲染；无 AI slop（禁 em-dash/emoji 图标滥用/紫渐变）。

## 5.1 已有实现 vs 设计稿对齐模式（回滚/修正）

当用户指出“你把某处样式改了 / 请先告诉我不一致的地方 / 跟设计稿对不上”时，这不是普通澄清，而是**对齐审计**。

必须执行：

1. **停止当前修改**，不要直接 patch。
2. **读源**：重新读取设计稿（MasterGo DSL / 截图 / 设计 token），必要时也读当前部署文件/线上页面。
3. **结构化差异清单**：按页面模块（Hero / 卡片 / Timeline / Reviews / FAQ …）列出不一致，每行包含：
   - 不一致项
   - 设计稿规范
   - 当前实现
   - 是否结构性改动（会牵一发而动全身的要标红）
4. **一次聚焦关键项**：如果清单很长，先圈出 2–3 个影响最大的结构性差异，向用户确认是否按设计稿回退。
5. **确认后再改**：等用户说“对”或明确选了要修哪几项，再动手。

## 6. 反模式（禁止）

- ❌ 未确认理解就出稿（最严重）
- ❌ 一次抛多个问题
- ❌ 忽视用户中途补充（补充=修正，不重新规划）
- ❌ 用历史项目配色/规范套新设计
- ❌ 把「模式入口」误读为「普通设置项」而不追问（Smart care 教训）
- ❌ 把设备参数与模式混为一谈而不确认数据归属
- ❌ 术语不一致不指出（同一概念多个名字 = 认知负担）
- ❌ **用户要求“先告诉我哪里不一致”时直接改代码（新增：必须先做差异审计并确认）**
- ❌ **默认稿做出国内促销 UI（滴滴/淘宝式）**：高饱和促销渐变 / Banner 轮播 / 金币券红包倒计时贴纸 / 红点轰炸 / 彩色图标堆砌 / 促销弹窗。欧美默认基线 = Apple、Uber、BirdBuddy（对照见 references/design-tokens.md）
- ❌ **一屏多个等权主 CTA 或装饰动效抢注意**（Hick 定律 + Apple HIG：一屏一个主操作，次要入口降级 ghost/链接）
- ❌ **未按目标市场选字体/文案基调**：欧美 App 用系统字体栈（SF Pro/Roboto）与英文口语文案；不要用中文产品 UI 的字体字重习惯套欧美界面

## 7. 主流 AI Agent 适配

本 skill 不绑定单一 agent。协议本体（澄清 → 设计）与文件结构通用，适配方式见 `AGENTS.md`：

| Agent | 接入方式 |
|---|---|
| Hermes Agent | 复制到 `~/.hermes/skills/`，工具自动发现 |
| Cursor | 根目录放 `.cursor/rules/` 或读 AGENTS.md |
| Claude Code | `CLAUDE.md` 引用 AGENTS.md 协议 |
| Codex / Cline | 读 AGENTS.md，按提示词加载 |

## 8. 参考

- 完整问题库：`references/问题库全表.md`（分场景 60+ 问 + 对抗式/断点/友好性清单 + 苏格拉底句式）
- 预置设计 token：`references/design-tokens.md`（默认值，用户可上传自定义 token 覆盖）
- 主流 AI agent 适配：`AGENTS.md`
- 使用说明（可分发）：`docs/使用说明.md`
